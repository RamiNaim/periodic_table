{
  "name": "elementtable",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx vite --config vite.config.js --host 0.0.0.0 src",
    "build": "npx vite build --config vite.config.js --emptyOutDir --outDir ../dist src",
    "build-gh": "gh-pages -d ./dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "three": "^0.152.2"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^4.3.5"
  }
}
